(defwidget bluetooth_control_center []
  (box
    :halign "start"
    :width 35
    (button
      :active {bluetooth-icon != "󰂲"} 
      :onclick "scripts/bluetooth blueman-manager"
      :class "bluetooth-control-center"
      :tooltip "${connected-device}"
      "${bluetooth-icon}"
    )
  )
)

