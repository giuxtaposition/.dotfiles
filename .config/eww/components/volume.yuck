(defwidget volume-module []
  (box
    (label
      :class "vol-icon icon"
      :text volume
      :tooltip "volume ${volume-percent}"
    )
  )
)

(defpoll volume			:interval "1s"	"scripts/volume icon")
(defpoll volume-percent :interval "1s"	"scripts/volume percent")
