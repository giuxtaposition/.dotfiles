(defwidget wifi []
  (button
    :class "icon wifi-icon"
    :tooltip wifi-name
    wifi-icon
  )
)

(defpoll wifi-icon :interval "1s" :initial "󰤭" "scripts/wifi icon")
(defpoll wifi-name :interval "1s" :initial "" "scripts/wifi name")
